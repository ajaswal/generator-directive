language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-directive' ]; then cd .. && eval "mv $currentfolder generator-directive" && cd generator-directive; fi

